run:
  name: smoke_tabular_transductive_laplace_learning_iris
  seed: 1
  output_dir: runs/transductive/laplace_learning/tabular/iris
  fail_fast: true
  log_level: detailed

limits:
  profile: auto

dataset:
  id: iris
  download: true
sampling:
  seed: 1
  plan:
    split:
      kind: holdout
      test_fraction: 0.2
      val_fraction: 0.1
      stratify: true
      shuffle: true
    labeling:
      mode: fraction
      value: 0.2
      strategy: balanced
      min_per_class: 1
    imbalance:
      kind: none
    policy:
      respect_official_test: true
      use_official_graph_masks: true
      allow_override_official: false
preprocess:
  seed: 1
  fit_on: train_labeled
  cache: true
  plan:
    output_key: features.X
    steps:
    - id: labels.encode
    - id: core.ensure_2d
    - id: core.pca
      params:
        n_components: 4
    - id: core.to_numpy
method:
  kind: transductive
  id: laplace_learning
  device:
    device: "auto"
    dtype: float32
  params: {}
evaluation:
  split_for_model_selection: val
  report_splits:
  - val
  - test
  metrics:
  - accuracy
  - macro_f1
graph:
  enabled: true
  seed: 1
  cache: true
  cache_dir: cache/graphs
  spec:
    scheme: knn
    metric: cosine
    k: 10
    symmetrize: or
    weights:
      kind: heat
      sigma: 1.0
    normalize: rw
    self_loops: true
    backend: numpy
    chunk_size: 256
    feature_field: features.X
