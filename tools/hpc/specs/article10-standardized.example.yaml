schema_version: 1
campaign_id: article10-standardized-v1
track: standardized
scientific_scope:
  claim_scope_id: article10
  stage: production
  claim_eligible: true
  gate_policy_id: modssc-scientific-gates-v2
  gate_policy_sha256: from_registry
default_site: slurm-gpu
selection:
  config_root: bench/configs/best
  dataset_lock_file: bench/campaigns/locks/article10-datasets.example.yaml
  methods:
    - pseudo_label
    - tri_training
    - democratic_co_learning
    - fixmatch
    - flexmatch
    - free_match
    - softmatch
    - laplace_learning
    - poisson_learning
    - grand
  seeds: from_config
code:
  git_sha: REPLACE_WITH_CLEAN_COMMIT
  require_clean: true
  environment_lock_sha256: REPLACE_WITH_ENVIRONMENT_LOCK_SHA256
expect:
  config_count: 1110
  task_count: 5550
  tasks_per_method: 555
  tasks_by_profile:
    a100_gpu: 3600
    v100_gpu: 1395
    cpu_graph: 555
  tasks_by_site:
    slurm-gpu: 4995
    regional: 555
profile_rules:
  - profile: cpu_graph
    site: regional
    methods: [poisson_learning]
  - profile: a100_gpu
    site: slurm-gpu
    methods:
      - pseudo_label
      - tri_training
      - democratic_co_learning
      - fixmatch
      - flexmatch
      - free_match
      - softmatch
      - laplace_learning
      - grand
    modalities: [vision, text, audio]
  - profile: v100_gpu
    site: slurm-gpu
    methods:
      - pseudo_label
      - tri_training
      - democratic_co_learning
      - fixmatch
      - flexmatch
      - free_match
      - softmatch
      - laplace_learning
      - grand
    modalities: [tabular, graph]
