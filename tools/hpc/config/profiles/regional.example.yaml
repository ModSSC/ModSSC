schema_version: 1
site_id: regional
scheduler: slurm
environment_lock_sha256: REPLACE_WITH_ENVIRONMENT_LOCK_SHA256
setup:
  - source "$MODSSC_ROOT/tools/hpc/sites/regional/job_env.sh"
profiles:
  cpu_tabular:
    architecture: CPU
    accelerators_per_task: 0
    concurrency: 32
    initial_concurrency: 32
    promoted_concurrency: 32
    max_walltime: "02:00:00"
    array_block_size: 256
    directives:
      account: REPLACE_WITH_ACCOUNT
      partition: REPLACE_WITH_PARTITION
      nodes: 1
      ntasks: 1
      cpus-per-task: 4
      mem: 16G
      time: "02:00:00"
  cpu_graph:
    architecture: CPU
    accelerators_per_task: 0
    concurrency: 16
    initial_concurrency: 16
    promoted_concurrency: 16
    max_walltime: "04:00:00"
    directives:
      account: REPLACE_WITH_ACCOUNT
      partition: REPLACE_WITH_PARTITION
      nodes: 1
      ntasks: 1
      cpus-per-task: 16
      mem: 64G
      time: "04:00:00"
