# Paper replication results

!!! warning "No accepted paper result yet"

    The Calder MNIST VAE2 execution is authorised, but there is currently no
    reconciled and immutable native paper bundle to list.

This namespace is reserved for article-specific replications. Each bundle must
identify the exact paper protocol, reproduction cards, source and environment,
authenticated data, complete declared seed set, reconciliation, acceptance
outcome, and external raw-archive digest.

Paper bundles preserve differences between publications, including datasets,
label budgets, split policies, preprocessing, metrics, and selection rules.
They must not be combined into a common leaderboard. Only fresh results from
the active 20 cards may enter this namespace.

The current candidate bundle is defined by the
[Calder MNIST VAE2 reconstruction v1](../../campaigns/calder-mnist-vae2-v1.md).
It will appear as `calder-mnist-vae2-v1/` only after the complete declared seed
sets, failures, identities, and external raw archive have been reconciled.
